# Local dev: use the Vite proxy (see vite.config.ts) to avoid CORS.
VITE_API_BASE_URL=/api/v1

# Optional: override the upstream API target for the dev proxy.
# VITE_API_PROXY_TARGET=https://fe-technical-assignment.dxtr.asia
